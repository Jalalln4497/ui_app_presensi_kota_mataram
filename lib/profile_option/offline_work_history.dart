import 'package:flutter/material.dart';

class OfflineWorkHistoryScreen extends StatelessWidget {
  const OfflineWorkHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Riwayat hari kerja Offline'),
      ),
      body: const Center(
        child: Text('Riwayat hari kerja Offline Page'),
      ),
    );
  }
}
